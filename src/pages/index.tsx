import { NextPage } from "next";
import styled from "@emotion/styled";

const Home: NextPage = () => {
  return (
    <Root>
      Hello, World!
    </Root>
  )
}

const Root = styled.div``;

export default Home;
